name: Supabase Health Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Check Supabase Project Status
        run: |
          curl -I https://pojmoiuzeckhxbnahcrk.supabase.co
